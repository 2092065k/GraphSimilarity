import numpy as np


def generate_graphs_file(name, num_of_graphs, num_of_nodes, seed,
                         weighted = False, weight_range = [1, 10],
                         dense_regions = [], sparse_regions = []):
    
    np.random.seed(seed)
    
    return 0
